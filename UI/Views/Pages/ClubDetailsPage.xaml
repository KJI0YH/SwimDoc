<Page x:Class="UI.Views.Pages.ClubDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      ScrollViewer.CanContentScroll="False"
      xmlns:loc="clr-namespace:UI.Localization"
      xmlns:dataGridView="clr-namespace:UI.Views.Controls.DataGridView">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TabControl Grid.Row="1" Margin="5">
            <TabItem Header="{loc:Localizastion Tabs_Athletes}">
                <dataGridView:AthletesView DataContext="{Binding AthletesTable}" />
            </TabItem>
            <TabItem Header="{loc:Localizastion Tabs_Entries}">
                <dataGridView:EntriesView DataContext="{Binding EntriesTable}"
                                          ShowImportActions="False" />
            </TabItem>
            <TabItem Header="{loc:Localizastion Tabs_Results}">
                <dataGridView:ParticipantResultsView DataContext="{Binding ResultsTable}" />
            </TabItem>
        </TabControl>
    </Grid>
</Page>
