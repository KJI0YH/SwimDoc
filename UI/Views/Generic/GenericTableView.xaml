<UserControl x:Class="UI.Views.Generic.GenericTableView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:generic="clr-namespace:UI.Views.Generic"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ContentPresenter Grid.Row="0"
                          Content="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Toolbar}"
                          Margin="5">
            <ContentPresenter.Style>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Toolbar}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ContentPresenter.Style>
        </ContentPresenter>

        <DockPanel Grid.Row="0" Margin="5">
            <DockPanel.Style>
                <Style TargetType="DockPanel">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=Toolbar}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DockPanel.Style>

            <ContentPresenter DockPanel.Dock="Right"
                              Content="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ToolbarContent}"
                              HorizontalAlignment="Right" />

            <generic:GenericTableDefaultToolbar />
        </DockPanel>

        <ui:DataGrid Grid.Row="1"
                  Margin="5"
                  x:Name="DgTableView"
                  ItemsSource="{Binding Items}"
                  SelectionMode="Extended"
                  SelectionUnit="FullRow"
                  AutoGenerateColumns="{Binding AutoGenerateColumns}"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="All"
                  HeadersVisibility="Column"
                  Loaded="DataGrid_Loaded"
                  SelectionChanged="DataGrid_SelectionChanged"
                  AutoGeneratingColumn="DataGrid_AutoGeneratingColumn">
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <EventSetter Event="Click" Handler="ColumnHeader_Click" />
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </ui:DataGrid>
    </Grid>
</UserControl>