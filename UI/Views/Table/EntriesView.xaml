<UserControl x:Class="UI.Views.Table.EntriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:generic="clr-namespace:UI.Views.Generic"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:components="clr-namespace:UI.Views.Components"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <generic:GenericTableView>
            <generic:GenericTableView.Toolbar>
                <DockPanel>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="{ui:SymbolIcon FolderAdd24}"
                                Command="{Binding ImportEntriesFromFileCommand}"
                                ToolTip="Загрузить заявки из файла"
                                Margin="5"
                                Padding="9" />
                        <generic:GenericTableDefaultToolbar />
                    </StackPanel>
                </DockPanel>
            </generic:GenericTableView.Toolbar>
        </generic:GenericTableView>

        <components:ImportInfoBar Margin="10"
                                 VerticalAlignment="Bottom"
                                 HorizontalAlignment="Stretch"
                                 Panel.ZIndex="1000" />
    </Grid>
</UserControl>