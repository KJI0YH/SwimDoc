<UserControl x:Class="UI.Views.Controls.DataGridView.ParticipantResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dataView="clr-namespace:UI.Views.Controls.DataView"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:loc="clr-namespace:UI.Localization"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <dataView:DefaultDataView>
        <dataView:DefaultDataView.MainContent>
            <ui:DataGrid ItemsSource="{Binding Results}"
                      SelectedItem="{Binding SelectedResult}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      RowStyle="{StaticResource DataGridRowResultsPlaceStyle}"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="{loc:Localizastion Results_Col_Event}" Binding="{Binding EventName}" Width="*" />
                    <DataGridTextColumn Header="{loc:Localizastion Results_Col_Participant}" Binding="{Binding ParticipantName}" Width="*" />
                    <DataGridTextColumn Header="{loc:Localizastion Fixation_Col_BirthYear}" Binding="{Binding ParticipantYearOfBirth}" Width="120" />
                    <DataGridTextColumn Header="{loc:Localizastion Results_Col_Team}" Binding="{Binding ClubName}" Width="*" />
                    <DataGridTextColumn Header="{loc:Localizastion Results_Col_Place}" Binding="{Binding PlaceDisplay}" Width="80" />
                    <DataGridTextColumn Header="{loc:Localizastion Results_Col_Result}" Binding="{Binding ResultText}" Width="150" />
                    <DataGridTextColumn Header="{loc:Localizastion Results_Col_Points}" Binding="{Binding Points}" Width="150" />
                </DataGrid.Columns>
            </ui:DataGrid>
        </dataView:DefaultDataView.MainContent>
    </dataView:DefaultDataView>
</UserControl>
