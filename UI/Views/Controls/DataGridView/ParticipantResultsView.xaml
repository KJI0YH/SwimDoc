<UserControl x:Class="UI.Views.Controls.DataGridView.ParticipantResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dataView="clr-namespace:UI.Views.Controls.DataView"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <dataView:DefaultDataView>
        <dataView:DefaultDataView.MainContent>
            <ui:DataGrid ItemsSource="{Binding Results}"
                      SelectedItem="{Binding SelectedResult}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      RowStyle="{StaticResource DataGridRowResultsPlaceStyle}"
                      MouseDoubleClick="DataGrid_MouseDoubleClick">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Событие" Binding="{Binding EventName}" Width="*" />
                    <DataGridTextColumn Header="Участник" Binding="{Binding ParticipantName}" Width="*" />
                    <DataGridTextColumn Header="Команда" Binding="{Binding ClubName}" Width="*" />
                    <DataGridTextColumn Header="Место" Binding="{Binding Place}" Width="80" />
                    <DataGridTextColumn Header="Результат" Binding="{Binding ResultText}" Width="150" />
                    <DataGridTextColumn Header="Очки" Binding="{Binding Points}" Width="150" />
                </DataGrid.Columns>
            </ui:DataGrid>
        </dataView:DefaultDataView.MainContent>
    </dataView:DefaultDataView>
</UserControl>
