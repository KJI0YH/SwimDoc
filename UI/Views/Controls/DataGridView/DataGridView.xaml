<UserControl x:Class="UI.Views.Controls.DataGridView.DataGridView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <ui:DataGrid Margin="5"
                 x:Name="DgTableView"
                 ItemsSource="{Binding Items}"
                 SelectionMode="Extended"
                 SelectionUnit="FullRow"
                 AutoGenerateColumns="{Binding AutoGenerateColumns}"
                 CanUserSortColumns="False"
                 CanUserAddRows="False"
                 CanUserDeleteRows="False"
                 IsReadOnly="True"
                 GridLinesVisibility="All"
                 HeadersVisibility="Column"
                 Loaded="DataGrid_Loaded"
                 SelectionChanged="DataGrid_SelectionChanged"
                 AutoGeneratingColumn="DataGrid_AutoGeneratingColumn"
                 MouseDoubleClick="DataGrid_MouseDoubleClick">
        <ui:DataGrid.InputBindings>
            <KeyBinding Key="Delete" Command="{Binding DeleteItemCommand}" />
            <KeyBinding Key="E" Modifiers="Control" Command="{Binding EditItemCommand}" />
            <KeyBinding Key="N" Modifiers="Control" Command="{Binding CreateItemCommand}" />
        </ui:DataGrid.InputBindings>
        <DataGrid.ColumnHeaderStyle>
            <Style TargetType="DataGridColumnHeader">
                <EventSetter Event="Click" Handler="ColumnHeader_Click" />
            </Style>
        </DataGrid.ColumnHeaderStyle>
    </ui:DataGrid>
</UserControl>