<UserControl x:Class="UI.Views.Controls.AddEditView.AddEditDialogContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             HorizontalAlignment="Stretch"
             Width="{Binding ContentWidth, FallbackValue=520}"
             MinWidth="{Binding ContentWidth, FallbackValue=520}"
             MaxWidth="{Binding ContentWidth, FallbackValue=520}">
    <StackPanel>
        <ContentControl HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        Content="{Binding EditorContent, RelativeSource={RelativeSource AncestorType=UserControl}}" />

        <Border Margin="0,12,0,0"
                Padding="12,10"
                CornerRadius="6"
                Background="#12C42B1C"
                BorderBrush="{DynamicResource SystemFillColorCriticalBrush}"
                BorderThickness="1">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Collapsed" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding HasErrors}" Value="True">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:SymbolIcon Grid.Column="0"
                               Symbol="ErrorCircle24"
                               Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                               Margin="0,0,10,0"
                               VerticalAlignment="Top" />
                <ItemsControl Grid.Column="1"
                              HorizontalAlignment="Stretch"
                              ItemsSource="{Binding ValidationErrors}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Foreground="{DynamicResource SystemFillColorCriticalBrush}"
                                       Text="{Binding}"
                                       TextWrapping="Wrap"
                                       Margin="0,1" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>
    </StackPanel>
</UserControl>
