<UserControl x:Class="UI.Views.Controls.RankTimesSettingsView.RankTimesSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:loc="clr-namespace:UI.Localization"
             HorizontalAlignment="Stretch"
             VerticalAlignment="Stretch"
             HorizontalContentAlignment="Stretch">
    <UserControl.Resources>
        <Style x:Key="RankTimesDataGridStyle" TargetType="ui:DataGrid" BasedOn="{StaticResource {x:Type ui:DataGrid}}">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="VerticalAlignment" Value="Stretch" />
            <Setter Property="AutoGenerateColumns" Value="False" />
            <Setter Property="IsReadOnly" Value="False" />
            <Setter Property="CanUserAddRows" Value="False" />
            <Setter Property="CanUserDeleteRows" Value="False" />
            <Setter Property="CanUserSortColumns" Value="False" />
            <Setter Property="CanUserResizeColumns" Value="False" />
            <Setter Property="HeadersVisibility" Value="Column" />
            <Setter Property="EnableRowVirtualization" Value="True" />
            <Setter Property="EnableColumnVirtualization" Value="True" />
            <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
            <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
            <Setter Property="VirtualizingPanel.ScrollUnit" Value="Pixel" />
            <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
            <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        </Style>
    </UserControl.Resources>
    <Grid x:Name="LayoutRoot"
          HorizontalAlignment="Stretch"
          VerticalAlignment="Stretch"
          SizeChanged="LayoutRoot_OnSizeChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Visible" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ShowHeader, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Command="{Binding DataContext.CloseRankTimesCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                    Padding="8,6"
                    ToolTip="{loc:Localizastion Common_Back}">
                <StackPanel Orientation="Horizontal">
                    <ContentControl Content="{ui:SymbolIcon ChevronLeft24}"
                                    Margin="0,0,6,0"
                                    VerticalAlignment="Center" />
                    <TextBlock Text="{loc:Localizastion Common_Back}" VerticalAlignment="Center" />
                </StackPanel>
            </Button>

            <StackPanel Grid.Column="1" Margin="16,0" VerticalAlignment="Center">
                <TextBlock Style="{StaticResource TitleTextBlockStyle}"
                           Text="{loc:Localizastion Settings_Tab_Ranks}" />
                <TextBlock Margin="0,4,0,0"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                           TextWrapping="Wrap"
                           Text="{loc:Localizastion Settings_Ranks_Description}" />
            </StackPanel>

            <Button Grid.Column="2"
                    Command="{Binding SaveCommand}"
                    Padding="16,8"
                    ToolTip="{loc:Localizastion Common_Save}">
                <StackPanel Orientation="Horizontal">
                    <ContentControl Content="{ui:SymbolIcon Save24}"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center" />
                    <TextBlock Text="{loc:Localizastion Common_Save}"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </Grid>

        <TabControl x:Name="RankTimesTabs"
                    Grid.Row="1"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"
                    SelectionChanged="RankTimesTabs_OnSelectionChanged">
            <TabItem Header="{loc:Localizastion Ranks_Group_ScmMen}">
                <ui:DataGrid x:Name="ScmMenGrid"
                             Style="{StaticResource RankTimesDataGridStyle}"
                             ItemsSource="{Binding ScmMenRows}" />
            </TabItem>
            <TabItem Header="{loc:Localizastion Ranks_Group_ScmWomen}">
                <ui:DataGrid x:Name="ScmWomenGrid"
                             Style="{StaticResource RankTimesDataGridStyle}"
                             ItemsSource="{Binding ScmWomenRows}" />
            </TabItem>
            <TabItem Header="{loc:Localizastion Ranks_Group_LcmMen}">
                <ui:DataGrid x:Name="LcmMenGrid"
                             Style="{StaticResource RankTimesDataGridStyle}"
                             ItemsSource="{Binding LcmMenRows}" />
            </TabItem>
            <TabItem Header="{loc:Localizastion Ranks_Group_LcmWomen}">
                <ui:DataGrid x:Name="LcmWomenGrid"
                             Style="{StaticResource RankTimesDataGridStyle}"
                             ItemsSource="{Binding LcmWomenRows}" />
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
