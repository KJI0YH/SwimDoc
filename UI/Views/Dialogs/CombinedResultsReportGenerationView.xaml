<UserControl x:Class="UI.Views.Dialogs.CombinedResultsReportGenerationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:loc="clr-namespace:UI.Localization"
             mc:Ignorable="d">
    <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
    
                <TextBlock Grid.Row="0"
                           Text="{loc:Localizastion Reports_Field_OutputFile}"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10" />
    
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
    
                    <TextBox Grid.Column="0"
                             Text="{Binding OutputFilePath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             MinWidth="320"
                             Margin="0,0,10,0" />
                    <ui:Button Grid.Column="1"
                               Content="{ui:SymbolIcon Folder24}"
                               Padding="9"
                               Command="{Binding BrowseCommand}" />
                </Grid>
            </Grid>
</UserControl>
